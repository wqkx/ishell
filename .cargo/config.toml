# 项目级镜像：rsproxy 同时镜像 index 与 .crate 下载（国内可达），
# 覆盖服务器全局的 tuna（tuna 仅镜像 index，下载仍走慢速 static.crates.io）。
# 仅作用于本项目目录，不影响用户全局环境。
[source.crates-io]
replace-with = "rsproxy-sparse"

[source.rsproxy-sparse]
registry = "sparse+https://rsproxy.cn/index/"

[net]
retry = 6
