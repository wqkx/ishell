# Rust 构建产物
/target

# 项目级 cargo 镜像配置（仅为加速国内构建，属本机/构建机环境，不应入库）
/.cargo

# 本机归档的可执行文件与截图
/dist

# 敏感信息：测试服务器私钥/地址，绝不入库
/server_info

# Claude Code 本地设置
/.claude

# 根目录参考截图（非源码）
/*.png

# 其它
.DS_Store
**/*.rs.bk
*.log
