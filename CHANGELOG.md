# Changelog

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [未发布]

### Added

- **一键把 AI 控制代理装到服务器上**：`ishell-mcp` 的 Linux x86_64 / aarch64 二进制现在直接
  内嵌在 iShell 里。在那台服务器的终端里右键 →「安装 AI 控制代理到这台服务器」，iShell 用
  已有的 SFTP 通道把它推到 `~/.ishell-mcp/bin/ishell-mcp`、置可执行位、跑一次 `--version`
  确认能执行，再把注册命令打进终端（不自动执行）。此前要手工下对架构、scp 上去、跑安装脚本，
  还得记着升级时重做一遍——现在版本一致由构造保证，「版本不一致，请重新部署」不可能再出现。
  发版构建须先编代理再编 GUI，见 BUILD.md。发版包里只有 Linux 版内嵌、且只嵌自己那条腿的
  架构（macOS/Windows runner 产不出 Linux 二进制）；没嵌时菜单项不显示，手工安装那条路不变
- **往终端里粘贴图片/截图**：剪贴板里是图片时，Ctrl+V（或右键粘贴）会把它存成 PNG，
  远端会话则上传到那台服务器的 `/tmp`，然后把**路径**打进终端。Claude Code 这类 AI CLI 认
  路径，会自己去读那个文件。终端协议里塞不进图片，而 AI 又常常跑在远端、读不到你本机的剪贴板，
  所以只能这么走

### Changed

- **AI / MCP 控制默认改为开启**（此前默认关闭、要去设置里勾一次再重启）。仍然只监听本机
  Unix socket（`0600`），不开任何网络端口；设置里可以关掉。**升级后没有显式关过这个开关的
  用户会自动启用**，随之每次 SSH 连接都会做一次 MCP socket 的反向转发——只对信任的服务器用
- **AI 新开自己的会话默认不再逐次弹框**（`open_session` 首次用某条已保存连接）。原先每步都问，
  实际效果是 AI 干活时连着弹好几个框、用户点到最后已经不看内容了。想要回：设置 →
  「AI 新开会话无需逐次确认」取消勾选。
  **写入你自己打开的会话不在此列**——那一档永远要当面授权，**没有任何设置能绕过**：
  AI 只能随便动自己开的会话。你自己的标签里可能有半截命令、sudo 提示符、连着生产库的 psql，
  往里打字是两路输入交织。（开发过程中曾把这一档也一起放开，随即按用户要求改回，并加了一条
  以此为名的门禁测试。）
  多开 iShell 时的「点窗口选实例」不受影响（那是选择器不是闸门，只有一个实例时本来就直接绑定）
- 粘贴文本时会按远端的 bracketed paste 状态套 `ESC[200~`/`ESC[201~`。此前不套，多行内容被
  shell/TUI 当成逐行**敲入**：粘一段脚本等于逐行执行了它，粘一段多行提示词给 AI CLI 会被拆成
  好几次提交

### Fixed

- **重连后恢复工作目录的那行 `cd` 是在「SSH 刚连上」的一刻直接打进去的**：那时候远端多半
  还在吐 MOTD/banner，甚至可能停在某个登录脚本起的程序或密码提示符上——这一行轻则被冲掉、
  回显原样留在屏幕上（此前连回显吞除都没做），重则被那个正在等输入的程序吃掉。这与「MCP
  配对标识自动注入」是同一类问题，而那边早就有一整套闲置判据。现在改成：连上时只记下意图，
  等 shell **真的**闲下来（终端不忙、远端输出与用户输入都静止 2s）再注入，并吞掉回显；
  **用户一动手就放弃**（他已经在用这个 shell 了），等超过 15s 也放弃——意图不会无限期挂着、
  几分钟后突然往用户 shell 里敲一行 `cd`。两处注入现在共用同一个
  `Session::shell_idle_for_injection`，判据不会再各自漂移。

  这套判据落地时踩了四个坑，一并处理（都只存在于未发布代码里，没有流出过）：
  **①** 意图是 `do_reconnect` 置下的、截止时刻却要到 `Connected` 才武装，中间那段
  `restore_cwd_until` 是 `None`——把「没武装」读成「已过期」的话，重连窗口里的第一帧就
  把意图丢掉了，功能一次都不会触发（判定统一走 `cwd_restore_expired`，并给整段加上
  「已连接」的门：断线横幅不挡渲染，那段时间本来就在每帧求值；`do_reconnect` 也会把
  **上一轮**的截止时刻清掉，否则连接抖动时新置下的意图会被旧截止时刻当场判成过期）；
  **②** 判据只在渲染该会话时被采样，重连若发生在后台标签页，意图会一直挂着没人过问——
  过期清扫因此挪到与标签页无关的每帧路径上，且「已过期」一律优先于「恰好闲下来」；
  **③** `cd` 与 MCP 配对标识挨得太近时会连着注入，而 `expect_echo` 是整体覆写，后一次
  武装把前一次的回显吞除冲掉，`cd '…'` 又原样漏回屏幕——`expect_echo` 现在记下武装时刻，
  「距上一次替用户敲键盘满 2s」成为注入判据之一（只挡同一帧不够：回显要走一个远端往返
  才回来，而注入既不更新「远端输出时刻」也不更新「用户输入时刻」，下一帧那几道「静止」
  判据仍然全部放行）；
  **④** 判据全是每帧轮询的，而 egui 空闲窗口不转帧，此前唯一能兜住的是那条**以 AI 控制
  为门**的 150ms 心跳：关掉 AI 控制的人会悄悄丢掉这个功能。现在意图挂着期间自己续帧，
  截止时刻也进了 `arm_timeout_repaint` 的 deadline 集合；
  **⑤** 最根本的一条：「本次连接以来一个字节都没收到」此前被 `output_idle_for` 当成
  「已经静止」。`do_reconnect` 会重建终端（那个时刻归零），而 `Connected` 在同一帧被排空
  ——`Connected` 只是「SSH 连上了」，此刻远端的 MOTD 还差一个网络往返。也就是说，前四条
  修好之后，注入会**必然**落在 `Connected` 那一帧，正是这套判据从一开始就要防的那一刻。
  现在「还没见过输出」算不静止：这道判据问的是「shell 闲在提示符上吗」，而一个字节都没
  见过的连接，我们根本还没见过它的提示符

- **跨机直连拷贝的临时公钥撤销失败，却仍然报告成功**（静态审计发现，安全残留）：worker 侧
  `let _ = untrust_temp_key(..)` 把错误整个吞掉，`untrust_temp_key` 内部又 `let _ =
  exec_status(..)` 连退出码都不看，于是无论远端怎么答，UI 一律当成「已撤销」。真失败时，
  一把带 `restrict` 的临时公钥就留在目标机的 `~/.ssh/authorized_keys` 里，而用户没有任何
  机会知道该去清。现在：退出码/传输错误都算失败并**重试一次**（撞上 sshd 的 MaxSessions
  是典型瞬时失败），仍失败则把原因和标记一起摆到目标会话的状态栏上，告诉用户去删哪一行。
  拷贝本身的成败不受影响（那由 `direct_result` 决定），但清理失败不再是静默的
- **端口转发的监听在一次 accept 出错后静默死亡**：`Err(_) => break` 一视同仁地退出循环，
  且**不发任何状态**——UI 上永远停在「监听中」，实际上一条连接都不会再被接受。最难受的是
  EMFILE/ENFILE（fd 用满），那通常只是别处一次短暂的句柄泄漏，退一步就能恢复。改成一律
  重试（100ms 退避）、连续 32 次才放弃，且放弃时如实发 `ForwardStatus { ok: false }`
- **粘贴图片时打进终端的路径没有按 shell 规则引用**（本次新增的贴图功能自带的缺陷）：
  远端路径是我们自己拼的、干净，但**本机**那条来自 `std::env::temp_dir()`——Windows 上
  几乎一定含空格（`C:\Users\Some Name\AppData\Local\Temp`），原样打进 shell 会被拆成
  两个词，AI 拿到半截路径、读不到那张图。现在按需加引号，风格按**会话类型**选
  （远端一律 POSIX，只有「本机」会话且 iShell 跑在 Windows 上才用双引号）

- **iShell 会往用户自己的会话里自动打进并执行一条命令**（0.19 的回归，用户报「通过 MCP
  发出的信息会输入到当前会话里面」）：多机配对标识的自动注入
  （` export ISHELL_MCP_TOKEN=…` + 回车）一直挂在「AI 控制已开启」这个开关下面。此前 AI 控制
  默认关闭，能走到那儿的人都是自己去设置里勾过的，尚可接受；本版把 AI 控制默认改成开启之后，
  同一个门就变成了「**每一个新连上的会话**都会在空闲约 2 秒后被自动敲进一条命令并回车」。
  回显本该被吞掉，但命令是真的在用户 shell 里跑了，吞除一旦错位就直接看见。
  现在它有了自己的开关「自动注入配对标识（多机共用 AI 服务器）」，**默认关**；那套拓扑之外
  的人完全不受影响，需要的人在设置里勾一下，或继续用「复制配对配置」手工写进 AI 的环境变量

- **进程列表看不到正在吃 CPU / 吃内存的进程**（用户报「CentOS 上 gmx 明明占满 CPU 和内存，
  进程里却不显示，有时又显示」）：远端只把「按**累计** CPU 时间排前 200 名」的进程发回来。
  两个后果：① 刚起来的 CPU 大户累计量远小于机器上一堆长命进程，直接被切掉，要等它慢慢攒够
  累计量才挤进来——这就是「有时又显示」；② 侧栏支持按内存% 排序，数据源却按 CPU 截断，
  吃内存不吃 CPU 的进程根本没进过这份列表。现在远端发「CPU 前 120 ∪ RSS 前 120」，客户端
  去重后也按两个方向各留前 40 的并集，不再只按 CPU 截断
- **收藏夹弹窗偶尔只剩两行左右的高度**（上一版按「数据被覆盖」修过一次，那是同名现象的
  另一个原因，这次是真的显示问题）：`ScrollArea` 的高度取 `min(可用高度, max_height)`，
  而弹窗 `Area` 给出的可用高度来自它**上一帧量到的尺寸**，两者咬合成一个只能变小的棘轮——
  头一回只有一两条收藏时被量矮，之后收藏变多也长不回来，最终卡在 egui 的 64px 下限
  （按 28px 行高正好「两行左右」）。而 `Area` 状态按 id 持久化，一旦卡住重开弹窗也不恢复。
  加 `min_scrolled_height` 顶住下限即可；顺带让弹窗的「下方放不下就翻到上方」重新生效
  （此前列表会自己缩到放得下，弹窗从不觉得空间不够）
- **文件名超出列宽时，鼠标悬停会显示两个一模一样的提示框**：egui 的 `Label` 在文字被省略时
  会**自己**再挂一个「完整文字」tooltip（`show_tooltip_when_elided`，默认开），和我们自己挂的
  那个叠在一起。关掉自带的，只留信息更全的那个（带软链目标/断链说明）。文档查看器标签栏的
  文件名同此
- **Ctrl+V 在 Claude Code 里粘贴图片没有任何反应**：egui-winit 在**按下**时就把 Ctrl+V 认成
  粘贴命令、只读剪贴板里的**文本**，读不到就连按键事件都不再往下发——终端彻底收不到这一下。
  现在靠松开时的按键事件补上：剪贴板里有图就走上面新增的「贴图」流程，连图都没有则发 0x16
  （Ctrl+V 的原字节，与普通终端一致）
- 系统信息探测的定时器改为 `MissedTickBehavior::Delay`：一次探测超过 2s 后不再补课式地连着
  探几次——进程 CPU% 是按两次采样的墙钟间隔做差分的，间隔被压到接近 0 会算出离谱的百分比
- **远程桌面下用 fcitx 打字/删除有时把整个程序冻住**：编辑器此前是「光标在视口里才上报
  `o.ime`」。`o.ime` 为 `None` 的那一帧，egui-winit 会调 `set_ime_allowed(false)`，而 winit
  在 X11 上的实现是 **XDestroyIC**，下一帧变回 `Some` 就是 **XCreateIC**——两个都是同步的
  XIM 往返：Xlib 把请求发给输入法之后阻塞等回复，**没有超时**。于是光标停在视口边缘上下
  滚动时，每一帧都在销毁并重建一次输入上下文；只要 fcitx 在其中某一次往返中途没了（崩溃、
  重启、远程桌面会话切换），事件循环线程就永远停在那一次调用里，整个界面冻住。
  改为**聚焦期间恒定上报**，输入上下文只建一次。终端那侧上报的矩形也钳进可见区：往回翻
  历史时光标会滚出视口，钳位之后这个值恒定，滚动期间不再产生 `XSetICValues`（同样是同步
  XIM 往返）。**说明**：这减少的是「正卡在 XIM 往返里」的机会窗口，不是给 Xlib 加超时——
  真在往返中途丢了输入法仍可能卡住，那是 Xlib XIM 的固有行为，见 README 的排查说明
- **用 fcitx 打字时整个界面冻住，根因已由用户抓到的栈坐实**：卡在
  `set_spot → XSetICValues → _XimRead → XIfEvent → poll(timeout=-1)`。把光标位置告诉输入法
  在 X11 上是一次**同步的 XIM 请求**，Xlib 发出去之后无限期等回复（**没有超时**），而这个
  调用就发生在画界面的那个线程上。输入法只要在这中间没了（崩溃、重启、远程桌面会话切换/
  重连），线程就永远停在那儿。新增设置**「输入法候选框跟随光标」**（启动选项里，默认开、
  即时生效）：关掉之后 iShell 恒定上报同一个坐标，而 winit 只在坐标真的变了时才发这条请求
  ——于是**一条都不会再发出去**，这条卡死路径彻底消失。代价只是候选框停在输入区左上角，
  中文照常能打。**远程桌面下建议直接关掉**
- **新增 `ISHELL_NO_VSYNC=1` 启动开关**，用于排查/绕开「最小化窗口之后整个界面像被挂起、
  AI 经 MCP 也操作不了」。读框架源码得到的线索：eframe 会给**最小化的窗口照样画帧并交换
  缓冲**（它判断可见性用的 `viewport.info.visible()` 在原生平台上没有任何一处赋值，恒为真），
  而默认 `vsync: true` 让 glutin 用 `SwapInterval::Wait(1)`——那次 `swap_buffers` 要等一个
  垂直同步，而一个已被图标化、合成器不再呈现的窗口很可能永远等不到。这是那条链路上**唯一**
  会阻塞的调用。开关关掉垂直同步后交换缓冲立即返回，不可能卡住；代价是连续滚动可能撕裂，
  故不设为默认。**这条尚未确认就是用户那个 bug 的真因**，见 README 排查一节里的判别方法
- **每帧的非绘制工作挪到 `App::logic`**（排空 MCP 请求、排空会话后台事件、续帧节奏）。
  eframe 只在窗口可见时调用 `App::ui`（`if is_visible { app.update(); app.ui(); }`），而
  `logic` 无条件调用——这些活本来就该在那儿。**说明**：用户报的「最小化 iShell 之后 AI 就
  操作不了」经查**不是**这条：原生平台上没有任何代码给 `viewport.info.visible` 赋值，
  `is_visible` 恒为真，`App::ui` 从不会被跳过（已用无头实测双向确认：把窗口 unmap、以及
  直接写 `_NET_WM_STATE_HIDDEN`，MCP 都是 0.10s 正常应答，改与不改一样）。所以这条算加固
  （Windows 上 `is_visible` 确实会变假），那个 bug 的真因仍在排查
- **新增 UI 线程卡死检测**（`src/stall.rs`）：真卡住时往 `crash.log` 写一条说明（现象、
  最可能的原因、典型栈、该试什么、以及去抓 gdb 栈的命令）——此前用户能提供的只有
  「它又卡死了」，连一行记录都没有。**只报告，不替用户改任何设置**：判定终究只是
  「一段时间没出帧」，不是「证明线程卡住了」，静默改用户设置配不上这个确定性。
  已知会把事件循环整个堵住、但不是卡死的调用（原生文件对话框——`rfd` 在 Linux 上是同步的，
  而调用点都在绘制代码里）会被显式圈掉，不参与判定；但圈起来也有上限：对话框开了十分钟还
  没返回同样会记一条（`xdg-desktop-portal` 没在跑时 `pick_files()` 永不返回，那也是界面
  永久冻住）。那条记录只陈述事实、把「你只是开着框去忙别的」和「portal 挂了」两种读法都
  摆出来，程序不下断言。
  探测**只在用户刚敲过键的 30s 窗口内**进行（那期间每 2s 主动请求一帧，活着的事件循环会在
  毫秒级响应），停手之后一次多余的唤醒都不做，因此不会把 egui 正常的「空闲几分钟不出帧」
  误判成卡死。已实测：注入一个冻住 UI 线程的桩 → 8s 后如实写入 crash.log，且不碰任何设置文件；
  空闲跑 45s → 什么都不写；文件对话框开着十几秒 → 不误报。判因时会看窗口是不是最小化/被遮挡：
  那种情况下卡住更可能在窗口与绘制路径上（合成器不再给图标化的窗口调度垂直同步，而 eframe
  仍会给它画帧并交换缓冲），日志据此给出不同的建议
- **输入法半路没了会留下一截擦不掉的拼音**：fcitx 崩溃/重启时 `Ime(Disabled)` 永远不会来，
  组字区间就一直挂着——终端上留着没提交的拼音，编辑器/单行框里那段陈旧区间还会被下一次
  组字拿去替换，替到别的地方。现在三处（终端、编辑器、单行输入框）都会在「失焦」或
  「收到裸文本输入却一条 Ime 事件都没有」时把没提交的组字撤掉。后一个判据可靠是因为 XIM
  组字期间按键会被输入法过滤，能收到裸 `Text` 就说明组字已经不在了。用户重启输入法之后
  界面是干净的、可以接着用，不必重启 iShell
- MCP socket 在配置目录还不存在时绑定失败（全新安装 + 默认开启时会撞上，现象是「默认开着但
  AI 连不上」，只有日志里有一行 ENOENT）：绑定前先建目录

## [0.18.0] - 2026-08-21

**数据安全专项。** 这一版修掉了一批「文件被无端修改/删除」的问题，其中
**「修改权限」会把文件清空** 是线上正在发生的数据丢失，建议尽快升级。

同时补上了一套数据安全回归测试（整棵树快照 + 点名式断言：除了点名的路径，多一个、少一个、
改一个字节都算失败），以及终端分包对拍、按键编码、字符边界等此前完全没有覆盖的地方。
测试从 190 条增加到 271 条（含 10 条需真实 sshd 的集成测试）。

### Fixed

**数据安全**

- **文件面板右键「修改权限」会把那个文件清空**（严重）：一条「只改权限」的 SFTP SETSTAT
  实际捎带了 `size=0`——`FileAttributes::default()` 并不是全空，`size`/`uid`/`gid`/`atime`/`mtime`
  都是 `Some(0)`，序列化时四个标志位全开，服务端按规范先 `truncate(path, 0)` 再 chown。
  于是文件当场变 0 字节、修改时间被抹成 1970，界面上只回一句「操作失败」，看不出文件已经没了。
  这也是长期以来「编辑器保存把文件写空」的**真正根因**——此前一直被诊断成「个别服务器的怪脾气」，
  其实是我们自己每次都在请求截断，任何守规范的服务器都会照做
- **上传覆盖远端文件时，中途失败会把原文件留成半截**：以前是直接以 TRUNCATE 打开目标再流式写，
  目标在第一个字节写进去之前就已经被清空，此后任何断线/取消/出错都不可恢复。改为事务写：
  字节先写进 `<目标>.ishell-part`，校验落盘长度无误后才把原文件挪到备份、换入、删备份，
  **成功换入之前绝不触碰目标文件**
- **上传覆盖会丢掉目标原有的权限位**：0755 的部署脚本被覆盖后变成 0644 直接不能执行，
  0600 的配置文件变成全局可读
- **收藏夹会被其他标签页覆盖**（表现为「明明收藏了五六个，点开只显示两行」）：同一台服务器开
  多个标签页时，每个标签页各自持有打开那一刻的收藏快照，增删时拿自己那份旧快照**整体覆盖**
  磁盘——A 标签加了四条，B 标签（还拿着旧快照）再加一条就把那四条抹了。现在改为读盘→改→写回。
  同一根因下还有一个：删除按下标删，下标是相对过时快照算的，会删掉你没点的那条，改为按路径删
- **撤销跨会话拷贝的临时信任时，会放宽远端 `~/.ssh/authorized_keys` 的权限**：从 0600 变成
  umask 默认值。在 umask 002 的机器上变成 0664（group 可写），sshd 的 `StrictModes` 会因此
  拒绝该账号的**全部**公钥认证，等于把人锁在门外
- **同机复制粘贴/拖拽移动会无提示覆盖同名文件**：现在遵循冲突策略（见 Changed）
- `~/.ssh/known_hosts` 在「接受变更的主机密钥」之后权限被放宽为 umask 默认值
- **钥匙串调用一旦 panic，已保存的密码会全部解不开**：并发名额只减不增，攒够两次之后
  主密钥被永久判定为「不可用」，所有已存密码的解密都退化为返回密文原串，表现为莫名的认证失败

**终端**

- **tmux 下的通知会弹两遍**：codex/Claude Code 在 tmux 里发的是 DCS 透传通知，SSH 若恰好把包
  切在通知正文之后、结尾终止符之前（只差两个字节），那条通知会被扫描两次
- **`clear` 的转义序列被分包切断时，回滚缓冲没有被真正清空**：清完还能往上滚出旧内容，
  光标也停在原处而不是回到左上角
- **`cargo build` / `rg` 之类的进度上报被当成了桌面通知**，还会顺带把该标签的裸响铃提醒
  永久打开——此后每次 shell 补全失败都弹提醒（`OSC 9;4` 是进度协议，不是通知）

**其它**

- 远端命令输出不再无上限地读进内存（大 PDF 渲染/全文提取可能产出上百 MB）

### Changed

- **同机复制粘贴、拖拽移动现在也遵循「目标已存在时的默认处理」**（传输窗口标题栏那个设置）。
  此前它只管下载/上传，同机操作一律无提示覆盖。**「覆盖」是默认值，行为与以前完全一致**；
  只有主动选了「跳过」或「重命名」的用户会看到变化。探测失败或 SFTP 不可用时一律报错中止，
  绝不退化成覆盖
- 覆盖已有远端文件现在需要对**所在目录**有写权限（事务写要在里面建临时文件再改名），
  权限不足时的报错会说明这一点


## [0.17.2] - 2026-08-09

输入法崩溃修复 + 通知准确性。中文输入相关的崩溃从 0.17.0 起就在（更早的 `prev_char_boundary`
问题存在更久），用中文的话建议升级。

### Changed
- **AI 通知只在「需要确认」或「任务完成」时才弹**：以前裸 BEL 响铃也算一条通知，而它和
  shell 补全失败、readline 报错是同一个字节，Claude Code 在既非等待确认也非任务完成时
  同样会响它——拿它当判据必然误报，这就是「通知不准确」的根因。现在只认发送方明确标注
  的类别（右键菜单「配置 AI 完成通知」装的 hook 会打 `ishell:need` / `ishell:done`），
  外加第三方主动发的 OSC 9/777（发这个序列本身就是在明确要求提醒用户）
- **通知卡片常态透明度 0.72 → 0.40**：只需要用余光察觉「有事」，看内容时鼠标移上去即可
- 设置里 AI 通知的档位说明同步更新：原先写的是「判不出类别的一律按需要处理提醒」，
  与新行为正相反；「全部（含任务完成）」也改名为「含任务完成」，因为裸响铃在任何档位
  下都不再提醒
- 系统通知改走 D-Bus（`org.freedesktop.Notifications`）而不是 `notify-send`：替换与关闭
  是 D-Bus 方法自带的能力，而 `notify-send` 要 libnotify 0.8.0 才有对应选项，Ubuntu 22.04
  上还是 0.7.9。没有 `gdbus` 时自动退回 `notify-send`（能发，但没有替换/自动消失）

### Fixed
- **同一会话的系统通知会在通知中心里越堆越多**：现在后一条原地替换前一条，始终只留最新
- **切回有通知的标签后，通知不会自动消失**（应用内卡片必须手动点掉、系统气泡一直挂着）：
  现在窗口在前台且切到该标签时，卡片与系统气泡一并消失。判据与「不弹」的规则严格对称，
  少了「窗口在前台」这一半就会把后台标签刚生成的通知在同一帧抹掉，那才是真丢消息
- **同一台服务器的两个标签，传输浮窗里看到的列表内容对不上、下载项还显示不全**：合并
  展示时的收集顺序跟着「当前是哪个标签」变（`same_server_idxs` 把活动会话排在最前），
  再叠加列表 50 条的显示上限，两个标签就各自保留了不同的一批。现在按传输建立时刻做
  跨会话的全局新→旧排序（同刻用 `(会话, id)` 兜成全序），与当前在哪个标签无关；被上限
  截掉的条数也在列表末尾明说，不再出现「顶部计数 63、列表只有 50」还不解释的情况
- **输入法（fcitx）异常时可能整个应用崩溃**：自绘 IME 的组字区间是跨帧保存的，而它指向的
  缓冲区可能在两条 `Preedit` 之间被别处整个换掉（编辑器撤销/重新加载、切目录重置路径框、
  对话框换类型）。原先只把偏移 `.min(len)` 钳到串长，挡得住越界、挡不住**落在中文/emoji
  这类多字节字符中间**——那一样是 panic。现在 IME 路径上所有偏移统一走新的
  `ui::ime_safe`，替换/插入被打包成不可能用错的整体操作
- 编辑器绘制里的 `byte_to_char` 同样会在非字符边界上切片 panic：组字期间光标一旦被陈旧
  区间推到多字节字符中间，崩溃点会出现在绘制代码里、完全看不出跟输入法有关
- 同一处的 `prev_char_boundary` / `next_char_boundary` 也是裸切片（前者连越界都没挡）：
  光标落到多字节字符中间后，**第一次退格或左右移动**就会崩
- **撤销/重做会把坏光标原样放回来**：`caret_before` 是另一个形状的缓冲区留下的偏移，原先
  只 `.min(len)`，拦得住越界拦不住落在字符中间。这是整条链的源头——只在取边界的 helper
  里吸附是不够的，调用方仍拿未吸附的光标去算删除长度，panic 只是从一处挪到另一处。
  现在源头吸附，同时把 `v_apply`（编辑器唯一改内容的地方）做成总函数兜底

### Added
- **一帧绘制 panic 不再终止进程**：`App::ui` 外层加 `catch_unwind`，接住后记日志、界面上
  给出「已恢复，建议保存后重启」的提示。SSH 客户端崩一次意味着所有会话断开、编辑器里未
  保存的内容全丢，代价远大于这一帧少画点东西。连续 30 帧、或累计 100 帧都崩则不再兜底、
  照常退出，避免一个画不出东西的窗口空转烧 CPU——两条上限缺一不可，只看连续次数会被
  「隔帧崩」绕过（每次恢复都把连续计数清零）
- **崩溃日志**：panic 的位置、消息、线程、版本号和调用栈追加写入
  `~/.config/ishell/crash.log`（上限 256 KiB，超了重来）。从桌面项启动时 stderr 无人可见，
  以前出事只剩「它崩了」一句话。文件 0600、目录 0700：切片越界类 panic 的消息里会带上出错
  的字符串片段，可能是正在编辑的远端文件内容或路径，多用户机器上不能让同机其他人读到

### Changed
- 发布构建改为 `strip = "debuginfo"`（原先是全 strip）：保留符号表，让 crash.log 里的调用栈
  和 `gdb -p` 抓到的卡死现场可读。二进制大约 +2.3 MB

## [0.17.1] - 2026-08-07

代码审查修复版。全部问题都存在于已发布的 0.17.0 里，其中一条会**丢数据**，建议尽快升级。

### Fixed
- **本机会话把文件复制粘贴回它自己所在的目录会把源文件清成 0 字节**（数据丢失）：
  目标与源同路径时走到 `fs::copy(src, src)`，而它以 truncate 打开目标。同一个守卫顺带堵住
  「把目录复制进自己的子目录」导致的无限递归。远端那条路走 `cp -a`，cp 自己会拒绝，
  故只有本机会话受影响
- **文件面板的文件夹排在了文件后面**：排序键写反，而降序分支的分组边界又因此算成 0
  把数组整体翻转——两个错误在降序下互相抵消，表现为切换排序箭头时文件夹在列表顶和底
  之间来回跳
- **`copy_to_remote` 传不了超过 32 MiB 的文件**：请求行的长度上限是用 `take` 实现的，而它
  套的是整条连接，把紧跟在请求行之后的文件流也一起限死了。真实分界线是 32 MiB 减去请求行
  长度，报错还指不到根因
- **MCP `ISHELL_MCP_SOCKET` 静默绕过配对 token**：一条过期的手工隧道 socket 会让多机隔离
  失效——命令照常执行，只是落到别人的电脑上。改为同时设了 token 就必须过握手
- **文件面板的视图缓存只在收到目录列表时失效**：手动刷新、新建的乐观插入、拖拽移动的乐观
  移除全都改不动界面。这正是「删掉文件夹后外部重建同名、刷新却不显示，过滤框反倒搜得到」
  的根因
- **系统通知里以 `-` 开头的正文被整条丢掉**：`notify-send` 缺 `--` 分隔符，而 AI CLI 常输出
  以 `-` 开头的 markdown 列表项
- **通知浮层显示的是最旧六条**：新通知追加在尾部，攒满后新的永远画不出来，只有「+N」在涨
- **OSC 通知被 SSH 分包切断时同时错两件事**：通知丢掉，且下一块开头那半截里给 OSC 收尾的
  BEL 被当成真响铃——通知没弹，反而多出一条内容不对的提醒
- **收藏夹弹窗被屏幕底边裁掉**：`popup_below_widget` 从不向上翻转，文件区工具栏本就靠近
  窗口下沿，于是收藏再多也只展开两三行。改为按条数动态计算高度、最多五行，放不下自动翻到
  按钮上方
- **编辑器切换编码/行尾后改回原值仍显示已修改**：关闭时会提示保存一个没动过的文件
- **保存文件时临时文件的权限窗口**：先按 umask 建再 chmod，0600 文件的明文有一瞬间全局
  可读；且权限设置失败被吞掉照样换入，等于把 `~/.ssh/config` 静默降成 0644
- **本机会话静默吞掉下载/上传请求**：传输面板留下一条永远 0% 的记录，去重逻辑还会对之后
  每次重试回答「已在下载中」，再也清不掉
- **本机会话关闭标签留下僵尸进程**：只 kill 不 wait
- **跨会话拷贝失败/超时后不停正在跑的传输**：出现「告诉 AI 超时了、远端文件其实写成功了」
  的分歧；且总超时后还会再起一次注定作废的中转

### Changed
- **MCP 配对 token 自动注入新增「本次连接以来没敲过键」前提**：原有信号分不清「shell
  提示符」和「程序阻塞在 stdin」，sudo/ssh 的密码提示符恰好也是安静不动的。连上就立刻
  打字的话本次不注入，退回原有的多实例弹窗选择
- **左侧栏右键菜单加宽**；**快速连接的「本机」改为双击打开**，与其下已保存主机一致

## [0.17.0] - 2026-08-01

### Added
- **AI 完成通知**：AI CLI 等待你确认或任务完成时，右上角浮出通知卡片；焦点不在 iShell 时
  同时发一条**系统通知**（Linux `notify-send` / macOS 通知中心）。设置里可选范围：
  **仅需要我处理时（默认）** / 全部（含任务完成）/ 关闭
- **右键「配置 AI 完成通知…」**：把一条安装命令**打进终端但不执行**，你看清了再回车。
  它给 Claude Code 的配置加两个 hook（备份原配置、按标记去重、保留已有 hook）。
  iShell 从不擅自改对方机器上的 AI 配置——这条路径是显式的、看得见的
- **编辑器字号缩放**：底部状态栏 `A- / 字号 / A+`，点数字复位；持久化

### Fixed
- **MCP 配对改为双向挑战-应答（协议 v4）**：v3 会把 `ISHELL_MCP_TOKEN` **明文发给每一条候选
  socket**——共用账号时那个目录里躺着别人的转发 socket，等于把密钥一次泄露给所有人。改为
  两侧互相出示 `HMAC(token, 随机数)`，**token 本身不再过线**；且必须**服务器先证**，否则一个
  连 token 都不知道的假 socket 就能把代理钓过去。⚠ 破坏性：GUI 与 ishell-mcp 必须同时升级
- **重连后的路径刷新只认 instance id**：而 id 并不是秘密，冒充者可在 SSH 重连的那一刻接管
  后续所有请求。配了 token 时改为重跑握手
- **版本不匹配被误报成「配对 token 不匹配」**：把用户引向一个怎么核对都没错的死胡同。探测
  改为三态，并只在没有任何同版本对端时才提示「重新部署」
- **DECCKM 被当成「终端忙」**：zsh 的 zle 每进一次行编辑就发 `smkx`（含 DECCKM），接受命令行
  时才复位——**空闲提示符下恒置位、真跑命令时反而复位**，语义完全相反。于是所有 zsh 用户的
  注入类功能永久失效，MCP 自动配对更是静默失效。去掉该信号（全屏程序另有备用屏覆盖）
- **`count_bel` 不认识 DCS**：tmux 透传形式（codex 在 tmux 里就这么发通知）里那个属于 OSC
  终止符的 BEL 被算成真响铃，同一条通知弹两遍。补齐 DCS/SOS/PM/APC 一族的跳过
- **`copy_from_remote` 落盘在 GUI 那台机器**：与 `copy_to_remote` 的「本地」不是同一台，
  「拉下来再推回去」必然报文件不存在。改为流式回传到 ishell-mcp 所在机器
- **编辑器点击行末之后的位置会跳到下一行**
- **窗口缩放时 codex 等自绘 TUI 把历史从头刷到尾**：拖拽窗口时每帧都真正 resize 并向远端
  连发 SIGWINCH。改为 130ms 去抖，尺寸稳定后才处理一次
- **Linux 桌面项非法**：`Version=` 按规范是「桌面项规范的版本」，而 bump 脚本每次发版都把
  应用版本写了进去，`desktop-file-validate` 直接报 error

### Changed
- 通知卡片重做：只在跑过 AI CLI 的标签才由裸 BEL 触发（普通 shell 的补全失败不再刷屏）、
  两行显示、半透明+悬停转不透明、同会话只留最新一条、层级降到传输浮窗之下
- 快速连接：「本机」收成一行、与其它主机同样大小；关闭按钮点击区放大（原先必须点正中心）
- 左侧右键菜单按主题收进子菜单（语言仍在顶层）
- 去掉右键菜单的「启用 AI 配对」——配对 token 已在会话空闲时自动注入
- 新生成的 MCP 配对 token 从 64 位加到 128 位（旧 token 继续可用）

## [0.16.13] - 2026-07-22

### Fixed
- **AI/MCP 控制下，窗口空闲后 MCP 请求会积压数分钟**：请求到达 socket 后由后台线程
  `request_repaint()` 唤醒 UI 线程处理，但 eframe 停在 `ControlFlow::Wait` 时跨线程唤醒会丢，
  请求只能干等到别的事件偶然唤起一帧——实测单条 `list_sessions` 卡 157 秒。且代理每次工具调用
  新开一条连接，故每个调用都可能中招。改用可靠的 `request_repaint_after`（OS 层定时唤醒，窗口
  被最小化/遮挡也照样触发）：仅在 AI 控制启用且有已连接会话时维持低频兜底节奏，保证请求
  ≤150ms 被处理；普通用户/无会话时零开销
- **同时下载多个文件时偶尔某个失败（网络无恙）**：每个传输各开一条新 SSH channel，6 路并发叠加
  基线 channel 与已完成传输 channel 关闭的回收延迟，瞬时并发数会偶尔撞上服务器 `MaxSessions`
  上限（OpenSSH 默认 10），第 11 条 `channel_open` 返回 `ConnectFailed`，而此前对该失败零重试，
  那个文件便永久失败。现对「服务器暂时给不出 channel」（`ChannelOpenFailure`）做有界退避重试
  （≤6 次、≤2s），等兄弟 channel 关闭腾出槽位即成；连接真死等其余错误仍立即失败、不空等

## [0.16.12] - 2026-07-21

### Security
- MCP 单条 JSON 请求上限从 256 MiB 下调到 32 MiB：整条请求作为一行读入内存，故该上限即「单连接
  请求缓冲峰值」。32 MiB 对 `write_file` 的文本/源码同步已足够（真正的大文件/二进制走
  `copy_to_remote` 的分块字节流，不塞进 JSON 行），又把「最多 32 条连接同时灌满大请求」的最坏
  并发内存从 ~8 GiB 压到 ~1 GiB。该 socket 可能经 SSH 反向转发暴露到远端主机，攻击面不止本机用户

### Fixed
- 文件列表的修改时间改为**逐时间戳**按当时真实的时区/夏令时换算：原先取「当前」UTC 偏移缓存起来
  套到所有文件，跨夏令时边界的历史文件会差 1 小时。现 unix 用 `localtime_r(secs)`、Windows 用
  `SystemTimeToTzSpecificLocalTime`，各自按该时刻真实的 DST 状态换算
- 设置保存失败（语言 / 下载目录 / MCP 同意态 / 主题 / 缩放等，因磁盘满 / 只读 / 权限没落盘）现在
  会弹顶部 toast 提示，不再是「UI 显示已切换、实际没保存、重启又变回旧值」的静默失败（尤其 MCP
  同意态这类安全相关设置，静默失败会造成开/关的认知偏差）

## [0.16.11] - 2026-07-21

### Security
- **同时开着多个 iShell 时，AI 可能在一次对话中途静默换一台机器执行命令**：代理进程此前按
  socket 文件的 **mtime** 挑实例（"谁后启动挑谁"），而且**每次工具调用都重挑一遍**。同时每个
  iShell 实例的会话 uid 都从 1 开始独立分配（`next_uid: 0` 起步），于是：AI 拿着实例 A 的
  `uid=1` 干活，用户中途新开了一个 iShell B，下一次调用就跟着跑到 B 上去了——而 B 里 `uid=1`
  同样存在，只是另一台完全不相干的主机。命令照跑，**不报任何错**。只读类操作（`read_screen`/
  `read_file`/`read_history`）不受授权门禁约束，会安安静静读错主机。
  现在：一个 `ishell-mcp` 代理进程**一辈子只绑定一个 iShell 实例**，绑定在首次连接时确定，
  之后永不静默改绑；每条请求都点名目标实例（`McpRequest.instance`），**由被叫的实例自己校验**
  （`McpRequest::is_addressed_to`）——校验放在权威侧而不是代理侧，代理的路径缓存无论怎么失效
  或指错，请求都到不了错误的实例身上。「没点名」一律拒绝，不当成「随便谁都行」。
  绑定的实例找不到了（比如它退出了）会明确报错，而不是顺手改绑到别的实例上。
- **AI 可以不经任何确认，直接往用户自己打开的会话里执行命令**（用户和 AI 同时能往同一个
  shell 里打字）：`open_session` 那道「让用户当面确认」的门禁只挂在开会话这一条路上，而
  `run_command`/`send_input`/`interrupt`/`write_file`/`copy_to_remote` 等全都是拿 `session_uid`
  直接找会话就干活——`ai_owned` 这个标记此前**只有 `close_session` 一处在检查**。于是 AI 只要
  `list_sessions` 看到用户已经打开的会话，直接 `run_command` 上去即可，完全绕过确认。雪上加霜
  的是 `McpSessionInfo` 根本没有 `ai_owned` 字段，AI 连"这是用户的会话、不该碰"都无从判断。
  而用户的会话没有 `ai_owned` 会话那层「不转发用户键盘输入」的只读保护，两路输入会在同一个
  shell 里交织：轻则互相打断，重则把 `run_command` 用来判断命令结束的哨兵标记行搅乱。
  现在：写入类操作若目标是用户自己打开的会话，弹窗让用户当面授权一次（授权按会话 uid 记住，
  内存态，重启 iShell 失效），弹窗里会写明 AI 具体要执行的命令/要覆盖的路径，而不是只说一句
  「AI 想操作这个会话」；`copy_between_sessions` 的源和目标两个会话都要授权（直连模式会往源
  主机落临时私钥、往目标主机的 `authorized_keys` 临时写一行，两边都在改远端状态）。只读操作
  （`read_screen`/`read_history`/`read_file`/`poll_run`）不受限——它们不碰用户的 shell、也不改
  远端状态。`close_session` 维持原样的**直接拒绝**（关闭权限不应该超过打开权限，不接受授权）

### Added
- **多开 iShell 时，用鼠标点窗口来决定 AI 操作哪一个**：代理发现本机有多个 iShell 实例时，
  会向每一个都发一条绑定请求，于是**每个 iShell 窗口上都弹出确认框**，用户在想用的那个窗口上
  点「允许」即可；代理拿到第一个应允就挂断其余连接，落选窗口的框自动消失，不用挨个去点拒绝。
  只有一个实例时直接绑定、全程无感，和以前一样。选择因此发生在用户的眼睛和鼠标上，而不是配置
  文件里——实例标识是纯内部的（`<pid>-<8 位随机>`），不出现在任何 UI 上、也不可配置：用户本来
  就是看着窗口决定的，不该逼他先给窗口取个名字再把名字念给 AI 听。`ISHELL_MCP_SOCKET` 保留
  作为脚本化场景的逃生口，指定了就不弹窗。
- `list_sessions` 返回值新增 `ai_owned` 字段，AI 终于能分辨哪些会话是用户自己打开的；工具
  描述与服务器说明也相应改口：此前写的是「list_sessions 看有哪些已打开的会话 → 需要的目标
  **不在列表里时**再 open_session」，等于明说「列表里有就直接拿来用」，现在改为默认给自己开
  专用会话、不要接管用户的窗口

### Removed
- **回退 0.16.10 引入的 vendor `egui-winit` 补丁**（连同 `vendor/egui-winit` 整个目录）：实测
  证明它在目标环境下**从未生效**，0.16.10 记载的根因是错的。真实根因不在 iShell 也不在
  egui-winit，而在输入法侧——fcitx4 的 XIM 模块默认 `UseOnTheSpotStyle=False`，此时它只向应用
  宣告 `PreeditPosition`/`PreeditNothing` 这类 style（语义是「组字文本由输入法自己画，不交给
  应用」）。winit 因此退回 root-window 模式，iShell 全程收不到**任何** `Ime::Preedit` 事件，
  那个补丁所在的分支自然永远执行不到。诊断方式：直接 `XGetIMValues(XNQueryInputStyle)` 把
  输入法**实际宣告**的 style 位图打出来，一次就问清了此前三轮推断都没问对的问题
- 用户侧解法与其代价（不涉及代码改动）：把 fcitx 的 `UseOnTheSpotStyle` 改为 `True` 并**完整
  重启** fcitx（该选项只在 XIM 服务器启动时读一次），Shift 丢字回显即消失；但随之而来的是
  候选窗口被钉死在窗口左下角、无法跟随光标——这是 libX11 在启用 preedit callbacks 时
  `XNSpotLocation` 失效导致的，见 freedesktop bug #1580（2004 年至今未修），winit 亦在
  `ime/context.rs::set_spot` 的注释里明确记载了这一限制。X11 + XIM 下二者不可兼得，两边都
  不在本项目可控范围内；Wayland 的 `text-input-v3` 没有这个包袱（eframe 已编入 wayland 特性）

### Fixed
- 滚动鼠标滚轮的过程中顺手按下 Ctrl，终端会莫名放大/缩小（几率不高）：Ctrl+滚轮缩放此前
  判断的是 `i.modifiers`（**本帧末**的全局修饰键状态）配 `smooth_scroll_delta`（**带惯性**，
  一次真实滚动之后会连续多帧持续吐出衰减的非零值）。于是「滚完轮子、惯性还在吐值的那几帧
  里按下 Ctrl」就会凭空触发缩放——要恰好按在惯性窗口内，所以几率不高。现改为只看本帧真实
  滚轮事件、并用事件**自带的**修饰键状态（`MouseWheel.modifiers` 记录的是该事件发生那一刻
  的按键状态）；顺带也修掉了「一次 Ctrl+滚动被惯性放大成连续多帧、每帧各缩放一级」的问题
- **AI/MCP 反向转发的 socket 在长连接下会被误删**：崩溃兜底清扫按 mtime 删「超过 24h 的残留
  socket」，但 Unix socket 文件的 mtime 是创建时刻、**不随流量刷新**——于是一条持续存活超过
  24h 的连接，其 socket 会被后来任一条新连接的清扫误删，导致该 iShell 实例在 MCP 里静默失联。
  现由活连接每 6h `touch` 自己的 socket 当心跳，mtime 变成「最后存活时间」：活的永远刷新、
  永不被误删；崩溃的停止刷新、超 24h 自然回收。单次 touch 失败（如通道打开撞服务端 MaxSessions）
  容错继续，不再一次瞬时失败就永久停掉心跳
- **文件列表刷新后偶发「外部新建的同名文件夹不显示、却能在过滤框搜到」**：同一目录可能有多个
  列目录请求在飞（删除后自动刷新 + 手动刷新 + 弱网超时重发），各自独立异步返回、可能乱序，而
  结果此前「谁最后到谁覆盖」——陈旧结果后到就盖掉了较新的列表。现给每个列目录请求打全局单调
  序号，只接受序号不更旧的结果、丢弃乱序陈旧结果；并顺带修掉「已有数据的目录其 loading 标记
  可能永久卡住、压制后续预取」的问题
- **文件夹压缩下载覆盖已存在目标时，中途失败可能连旧目标一起丢失**：此前是「先删旧目标、再把
  解压好的新目录 rename 上去」，一旦删成功、rename 失败就两头皆空、旧数据不可恢复。现改为「旧
  目标先 rename 到同目录备份名（原子、可回滚）→ 换上新目录 → 成功才删备份、失败则把备份换回」，
  保证成功前不破坏原目标
- **导入 `~/.ssh/config` 里的 IPv6 ProxyJump 会生成错误配置**：跳板机地址此前用 `split_once(':')`
  在第一个冒号处拆 host/port，`[2001:db8::1]:2222` 被拆成 host=`[2001`、端口解析失败静默回退 22，
  存成一条连不上或连错端口的跳板机。现正确处理方括号形式 `[v6]:port` 与裸 IPv6 字面量
- 端口转发监听地址对 IPv6 字面量改用结构化构造（`[::1]:8080` 而非手工拼接的 `::1:8080`），避免
  UI 显示与严格解析处因缺方括号出错
- 一批设置（语言/下载目录/强制 X11/MCP 同意态/主题/缩放等）保存失败此前被 `let _ =` 静默吞掉——
  磁盘满/只读/权限导致没落盘时，UI 显示已切换、重启却变回旧值且毫无线索。现失败会 `log::warn!`
  记录（尤其 MCP 同意态这类安全相关设置至少可查证），UI 层弹提示留作后续

### Added
- 终端新增按键/文本/IME 事件的诊断日志（`RUST_LOG=ishell=trace`，只记这三类不刷屏）：
  输入法类问题光看现象只能靠猜，必须先知道输入法到底发了什么、走的哪条通道
- **MCP 协议版本握手**：GUI 与 `ishell-mcp` 代理的线协议是配套编译的、必须同版本。代理现在在
  连接时校验对端 iShell 的协议版本（`MCP_PROTOCOL_VERSION`），不一致就明确报「请重新部署
  ishell-mcp」，而不是静默出错——根治「升级了 iShell 却忘了同步更新代理」这类隐患。
  `ishell-mcp --version` 会打印其 crate 与协议版本
- **代理二进制的标准安装位置 `~/.ishell-mcp/bin/ishell-mcp`** 与一键脚本 `scripts/install-mcp.sh`
  （临时名 + 原子 rename，避免覆盖运行中的二进制报 "Text file busy"，并打印 `claude mcp add`
  注册命令）。README（中/英）与 BUILD.md 更新为新装法，BUILD.md 补充了用 zig/cargo-zigbuild
  把代理交叉编译到其它 Linux 架构的说明（macOS 因需 Apple SDK 仍走原生/CI 构建）

## [0.16.10] - 2026-07-16

### Fixed
- **中文输入过程中按下 Shift，已输入的拼音会从输入框里消失**（再敲/删一个字符才重新冒出来），
  软件内所有输入框（终端、编辑器、路径栏、各对话框）都受影响。根因在上游 `egui-winit`：
  它把「带文本但没带光标位置」的组字事件（`Ime::Preedit(text, None)`）里的 text 整个丢掉，
  并当作组字结束推送 `ImeEvent::Disabled`；而 X11/fcitx 在组字**进行中**按下 Shift 等修饰键
  时发的正是这种事件——组字并没结束，只是这一拍不报光标位置。应用侧收到 Disabled 就把待
  组字文本从输入框移除，于是拼音「消失」；下一次按键带来带光标的 Preedit（内容是完整串），
  之前的字符又一起冒出来。现 vendor 一份 egui-winit 打补丁（同 glutin/vt100 的既有做法，
  见 `vendor/egui-winit/ISHELL_PATCH.md`）：组字文本非空即照常转发 Preedit，仅空文本才当
  组字结束；upstream 针对 macOS backspace 的分支不受影响
- 同时连着多个会话时，打开的文件可能被填进**另一个会话**的编辑器标签里（内容/路径来自
  错误的服务器）：打开文件的请求 id 取自各会话**独立**的 `next_xfer` 计数器，跨会话必然
  重号，而编辑器标签是全局一张表，回填时却只按 id 找标签、没校验会话。两个文件同名同内容
  时表面完全看不出来，但保存会写到**错误的服务器/路径**。现在内容就位、下载进度、加载失败、
  PDF 页数、docx 字节与其后台解析结果等各条回填路径统一按 `(uid, id)` 匹配

## [0.16.9] - 2026-07-16

### Fixed
- **编辑器粘贴含中文的内容会整个应用闪退**（未保存的改动一并丢失）：编辑器同一帧里的顺序是
  「按内容版本重算 lint 缓存 → handle_input 处理粘贴改内容 → 绘制」，于是本帧绘制用的仍是
  改动**之前**算出的 `lint_ranges`——那是一组旧内容的字节偏移。纯 ASCII 时旧偏移顶多让下划线
  画错一帧（每个字节都是字符边界），内容含中文等多字节字符时，旧偏移会落进某个字符中间，
  高亮器按该边界切片直接 panic（`end byte index N is not a char boundary`）。现把这些按内容
  版本缓存的重算（lint / 跨行高亮状态 / 行缩进）移到 `handle_input` 之后、绘制之前，缓存与
  被绘制的内容永远同一版本；同时给高亮器自身加了字符边界吸附作为防御网——UI 绘制路径上的
  panic 会连带丢掉用户未保存的改动，代价远高于「下划线范围略有出入」
- 从终端复制被「软换行」折断的长行时，粘贴出来凭空多了换行符，把一条命令/URL 拆断：
  选区取文本时无条件在屏幕行之间补 `\n`，没区分「终端因行满自动折行」与「真实收到 \n」。
  现按 vt100 的每行 wrapped 标志区分——软换行只把两行首尾相接（且不 trim 行尾，避免刚好
  在行尾的空格被吃掉导致粘连），真实换行才补 `\n`
- 在 iShell 终端里跑 Claude Code 等 TUI 时大量快捷键失灵：按键编码此前只处理了
  `Ctrl+字母`，其余修饰组合一律被丢弃或干脆不发。现补齐 xterm 标准编码——
  - **Shift+Tab** 发 `CSI Z`（back-tab），此前发的是普通 `\t`，于是 Claude Code 里用来切换
    权限模式（plan / auto-accept）的 Shift+Tab 变成了 Tab 补全
  - **Shift/Alt+Enter** 发 `ESC CR`，用作"换行但不提交"（裸 xterm 编码下它和回车都是 CR、
    无法区分，这也正是 Claude Code 的 `/terminal-setup` 要给别的终端写入的映射；iShell 自己
    就是终端，现已内建，用户无需再配）
  - **修饰键 + 方向键 / Home / End** 发 `CSI 1;<m> X`、**+ Insert/Delete/PageUp/PageDown**
    发 `CSI <n>;<m> ~`，此前修饰键被丢弃、降级成无修饰的普通方向键（Ctrl+←/→ 按词跳转、
    Shift+←/→ 选择因此失效）
  - **F1–F12**（含修饰）此前完全不发
  - **Alt+字母/数字** 发 `ESC <char>`（Meta 惯例：Alt+B/F 按词移动、Alt+D 删词），
    **Alt+Backspace** 发 `ESC DEL` 删词；macOS 上按住 Alt 时同时产生的文本事件（Option+B
    会输入 "∫"）随之抑制，即"Option 当 Meta 键"，但 AltGr（被报成 Ctrl+Alt）仍照常输入字符
  - **Ctrl+Space/[/\\/]//-** 发对应控制字符（NUL / ESC / FS / GS / US）

## [0.16.8] - 2026-07-16

### Fixed
- 编辑器保存在弱网/连接静默失联（如 Windows 上笔记本休眠唤醒、Wi-Fi 切换、VPN 抖动导致
  TCP 连接死掉但未收到 RST）时可能永久卡在"保存中"：此前保存请求发出后只等
  `FileSaved`/`FileSaveFailed`/`FileSaveConflict` 三种结果之一，没有任何超时兜底，
  远端 worker 迟迟不返回时标签会一直显示保存中、"珊瑚→绿"动画也卡在中途不再推进。
  现在保存关联一个 30 秒截止时间，超时则解锁标签为可重试状态并弹"保存超时，请检查网络
  连接"提示；每次保存分配一个独立 id，超时后姗姗来迟的旧结果会被安全识别并丢弃，
  不会误更新一个已经关闭或已重新保存过的标签
- 粘贴文本（尤其是从 Windows 记事本/Office/浏览器复制的内容，几乎总是 CRLF 换行）未做
  换行符归一化就直接插入编辑器内容，与内部"统一 LF"的约定不符，导致保存后的文件混入
  裸 `\r`、且可能让"内容是否变化"的判断被这些杂散字符干扰
- `copy_from_remote` 与 `copy_to_remote` 对"本地"的解析点不一致：前者一直是在 iShell GUI
  所在机器上落盘，后者是在运行 `ishell-mcp` 的调用方机器上——两个工具的"本地"其实不是
  同一台机器，导致"先 `copy_from_remote` 下载、再 `copy_to_remote` 读同一路径"这类中转
  操作会莫名报"文件不存在"。现在 `copy_from_remote` 也改为在调用方机器本地流式落盘，
  与 `copy_to_remote` 对称；目前流式模式仅支持单个文件，目录请多次调用或改用
  `run_command` 执行 `tar`/`rsync`
- AI/MCP 反向转发会在每次连接/重连时于远端主机 `$HOME` 根目录下新建一个
  `.ishell-mcp-<nonce>.sock` 文件，且从未清理，长期使用会在主目录堆积大量残留文件；
  现在统一放进 `~/.ishell-mcp/` 子目录，连接正常断开时主动删除本次注册的文件，
  另外每次连接前顺手清理这个子目录里 mtime 超过 24 小时的旧文件兜底崩溃场景
- AI/MCP 流式文件传输（`copy_to_remote`/`copy_from_remote`）此前直接以截断方式打开目标
  文件，一旦传输中途失败（断线/超时/源文件变化）就会在远端或调用方本机留下一个空/半截
  文件、破坏同名原文件；现在改为「写临时文件→校验字节数→原子换入」的事务写，失败即
  原文件分毫未动
- `copy_between_sessions` 直连模式下，多个拷贝并发写同一台目标主机的 `authorized_keys` 会
  互相踩：撤销临时信任的「读→改→写」若与另一次授信的追加/另一次撤销交错，会丢标记（临时
  公钥残留）或互相覆盖，且此前用的是固定临时文件名还会撞名；现在授信/撤销用同一把 `flock`
  串行化，临时文件名另加随机后缀双保险

### Added
- 新增 `copy_between_sessions` 工具：把一个已打开远端会话上的文件复制到另一个已打开远端
  会话，不需要先下载到本机再上传。优先尝试源主机直连目标主机（不经过运行 iShell 的机器
  中转，适合两台主机同一局域网/集群的场景）——会生成一个仅限本次使用的一次性密钥对，
  临时授权源主机免密连接目标主机，传输完成后立即撤销这个临时授权、删除临时密钥，不留
  长期可用的免密信任；如果直连不可行（网络不通、权限受限等），自动降级为经 iShell 进程
  内存中转（不落盘任何一方磁盘）。当前仅支持单个文件

### Changed
- AI/MCP 文件传输支持多文件同时传输：此前同一会话同一时刻只允许一个文件读写操作，多文件
  只能串行；现在放开为每会话最多约 16 个并发（SFTP 天然支持并发，worker 侧本就有并发+
  排队），AI 可以对同一会话并行发起多次 `copy_to_remote`/`copy_from_remote` 一次性传多个
  文件，超过上限才返回「并发已达上限」提示退避
- 文件列表工具栏调整为「后退 / 刷新 / 复制 / 收藏」四个按钮，收藏放在最后；后退图标从
  线箭头换成尖括号样式，收藏图标从系统字体的 ☆/★（笔画偏细、与其它按钮不搭）换成同一套
  phosphor 图标字体的 STAR（笔画与刷新/复制一致），已收藏用强调色标识

## [0.16.7] - 2026-07-15

### Fixed
- AI/MCP `poll_run` 在对端（`ishell-mcp` 进程）因其自身调用超时提前断开连接后，主进程侧仍把
  那次等待计为"占用中"，导致后续 `poll_run` 一直被"已有一个 poll_run 在等待"拒绝，只能靠
  `interrupt`（会中断正在跑的命令）才能恢复；现在连接一断开就能识别出这是孤儿等待者并自动放行

### Changed
- `write_file` 工具说明补充"二进制文件请用 copy_to_remote"提示；服务器说明补充 `&` 优先级
  低于 `&&` 的提醒，避免 `md5sum f && nohup cmd &` 这类写法把整条 `&&` 链一起丢进后台

## [0.16.6] - 2026-07-14

### Fixed
- v0.16.5 的 Windows CI 失败：新增的路径校验单元测试用 POSIX 风格路径（如 `/tmp/x`）断言
  `Path::is_absolute()` 应为真，但该判定标准随平台而变——Windows 下没有盘符前缀的路径不算
  绝对路径，导致测试在 Windows 上失败；这几个用例现在只在 unix 上跑（本来这条 MCP 通道也
  只在 unix 上真正启用）

## [0.16.5] - 2026-07-14

### Added
- AI/MCP 新增 `copy_to_remote` / `copy_from_remote` 工具：本地与远端之间直接复制文件/目录
  （复用现有 SFTP 上传/下载通道），字节不经过 MCP 连接本身——大文件、整个目录应该用这两个，
  而不是要求把全部内容内联进请求/响应 JSON 的 `write_file`/`read_file`

### Fixed
- `copy_from_remote` 拉取目录且要求改名时，压缩下载分支落地路径固定沿用远端目录名，
  实际不会改到调用方要求的本地目标名（报告成功但目标路径其实不存在）
- 目录下载覆盖已存在目标时是合并语义（保留目标里远端已不存在的旧文件），与预期的
  「覆盖」不符；现在统一为镜像覆盖：目标下载完成后只包含这次拉取的内容
- `copy_to_remote` 的本地路径存在性检查此前是同步 `std::fs::metadata`，在 UI 线程逐帧执行，
  路径落在慢速/挂起的挂载点时会卡住整个界面；改为交给 worker 侧本就异步的检测
- `copy_to_remote`/`copy_from_remote` 的路径校验从只判断"是否绝对路径"加强为拒绝空文件名
  （如 `/`、`////`）和 `.`/`..` 路径段，避免早前相对路径被静默当成写到文件系统根目录

## [0.16.4] - 2026-07-14

### Changed
- 应用图标与 README 顶部 logo 换新（新的像素风 3D 立体字 wordmark），统一加上圆角，
  同步更新 `assets/icon.png` / `assets/icon.ico` / `assets/macos/icon.png` / `docs/logo.png`

## [0.16.3] - 2026-07-14

### Fixed
- htop 等启用 DECCKM（应用光标键模式）的全屏程序里，方向键被误判成改 NI 参数：终端此前
  无论远端是否启用该模式都只发普通形式的方向键/Home/End 转义序列；现按 DECCKM 状态切换
  SS3/普通两种编码，和真实终端行为一致

### Changed
- README 移除首屏大图，版本号更新

## [0.16.2] - 2026-07-14

### Changed
- README 将 AI/MCP 提升为核心功能介绍（此前只在文末一段容易被忽略）
- CI 发布流程一并打包 `ishell-mcp`（Linux/macOS 四个平台）

## [0.16.1] - 2026-07-14

### Fixed
- AI/MCP 本地 IPC 此前只在 Linux 上编译测试过，Windows CI 因 tokio 的 Unix domain socket
  不可用而编译失败；按平台 `#[cfg(unix)]` 分离，Windows 上该特性直接返回明确的
  "暂不支持 Windows" 提示，其余代码正常编译

## [0.16.0] - 2026-07-14

### Added
- **AI/MCP 终端控制通道**：让 AI 助手（如 Claude Code）复用已打开的终端会话执行命令，
  而不是每次另开一条丢失 cwd/环境/历史的 `ssh host cmd`；支持通过已有 SSH 连接自动反向
  转发到远端、AI 专属只读会话（`open_session`/`close_session`）、`read_history` /
  `list_saved_connections` / `send_input` / `write_file` / `read_file` 等工具，默认关闭

### Fixed
- 弱网下鼠标滚轮/本地回滚逐帧取整丢量，触控板小幅滚动被吞掉
- resize 与清屏（`ESC[2J`+`ESC[3J`）重建 vt100 parser 时静默丢失鼠标上报等私有模式，
  全屏 TUI 一次窗口缩放后滚动即失效
- SSH 连接握手（TCP + 密钥交换）无超时保护，弱网下可能无限期挂起，导致「第一次连不上、
  后面怎么点重连都连不上」
- 文件目录负缓存永不失效：目录被外部进程频繁改动时，一次「不存在」判定会一直挡住导航，
  只能手动刷新父目录才能恢复
- 文件传输窗口文件名过长时与右侧状态图标重叠
- macOS 上 `Apple Color Emoji.ttc` 体积可达 150~200+ MB，整份读入常驻内存是空载占用
  数百 MB 的直接原因，现按体积跳过超大 emoji 字体
- 全屏 TUI（htop/less 等）备用屏滚动兼容：无鼠标上报时把滚轮转发为方向键

## [0.15.1] - 2026-07-13

### Fixed
- 窗口长时间最小化后内存暴涨：系统信息快照原先挤在无界 mpsc 队列里，UI 不排空
  （最小化时 egui 基本停止调用 update）就一直堆积；改为 watch 通道只保留最新一份，
  内存占用与最小化时长无关，恢复窗口也不再有「积压爆发式重绘」
- 终端拖拽选区起点漏选按下字符；滚动条边缘拖入文本区时选区路由误判
- 标签栏拖拽重排时起始瞬间的位置跳动（抓取偏移改用真实按下位置）

## [0.15.0] - 2026-07-10

### Added
- 终端真彩色（24-bit RGB）与 Unicode 宽字符渲染
- 大文件打开阈值与只读提示；路径面包屑导航
- 发布流程：`scripts/bump-version.sh`、`RELEASE.md`、CI 版本一致性校验

### Fixed
- 编辑器关闭标签时清理 egui `TextEditState`，修复大文件关闭后内存滞留
- 关闭非当前标签时 `active` 索引错位（编辑器与看图工具）
- 保存并关闭、加载失败等路径统一走 `remove_tab_at`

### Changed
- 大规模模块拆分：所有 `src/**/*.rs` 控制在约 600 行以内
- CI clippy 策略收紧；`rustfmt` 全仓格式化

## [0.14.4] - 2026-07-05

数据安全与评审整改（保存校验、剪贴板、路径框等）。详见 GitHub Release。

[0.15.0]: https://github.com/wqkx/ishell/releases/tag/v0.15.0
[0.14.4]: https://github.com/wqkx/ishell/releases/tag/v0.14.4
